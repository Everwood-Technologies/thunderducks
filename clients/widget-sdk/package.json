{
  "name": "@thunderducks/widget-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "iframe + postMessage widget SDK (deny-by-default)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/security.test.js dist/host.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
