{
  "name": "@thunderducks/web",
  "version": "0.1.0",
  "private": true,
  "description": "Thunderducks minimal TS web client (Wave E2)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/smoke.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
